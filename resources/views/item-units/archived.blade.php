<x-app-layout><x-slot name="header"><div><p class="text-sm font-medium text-green-700">Inventory</p><h2 class="text-2xl font-bold">Archived Equipment Units</h2></div></x-slot><div class="space-y-5"><form class="flex gap-2 rounded-2xl border bg-white p-4"><input name="search" value="{{ $search }}" placeholder="Search asset, serial, property..." class="flex-1 rounded-xl border-gray-300"><button class="rounded-xl bg-green-700 px-5 font-semibold text-white">Search</button><a href="{{ route('item-units.index') }}" class="rounded-xl border px-5 py-2.5">Back</a></form><div class="overflow-hidden rounded-2xl border bg-white"><table class="min-w-full divide-y"><thead class="bg-gray-50"><tr><th class="px-5 py-3 text-left">Asset</th><th class="px-5 py-3 text-left">Item</th><th class="px-5 py-3 text-left">Archived</th><th class="px-5 py-3 text-right">Action</th></tr></thead><tbody class="divide-y">@forelse($units as $unit)<tr><td class="px-5 py-4 font-bold">{{ $unit->asset_number }}</td><td class="px-5 py-4">{{ $unit->item?->display_name ?? 'Archived item' }}</td><td class="px-5 py-4 text-gray-600">{{ $unit->deleted_at?->format('M j, Y g:i A') }}</td><td class="px-5 py-4 text-right"><form method="POST" action="{{ route('item-units.restore',$unit->id) }}">@csrf @method('PATCH')<button class="font-semibold text-green-700">Restore</button></form></td></tr>@empty<tr><td colspan="4" class="p-12 text-center text-gray-500">No archived units found.</td></tr>@endforelse</tbody></table></div>{{ $units->links() }}</div></x-app-layout>
