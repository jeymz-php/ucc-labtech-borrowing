<x-app-layout>
    <x-slot name="header"><div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p class="text-sm font-medium text-green-700">{{ $itemUnit->item->item_code }} / Equipment Unit</p><h2 class="text-2xl font-bold text-gray-900">{{ $itemUnit->asset_number }}</h2></div><div class="flex flex-wrap gap-2">@can('print item barcodes')<a target="_blank" href="{{ route('item-units.print',$itemUnit) }}" class="rounded-xl border border-gray-300 px-4 py-2.5 font-semibold">Print Label</a>@endcan @can('edit items')@unless(in_array($itemUnit->availability_status,['reserved','borrowed']))<a href="{{ route('item-units.edit',$itemUnit) }}" class="rounded-xl bg-green-700 px-4 py-2.5 font-semibold text-white">Edit Unit</a>@endunless @endcan</div></div></x-slot>
    @if(session('success'))<div class="mb-6 rounded-xl border border-green-200 bg-green-50 p-4 text-green-800">{{ session('success') }}</div>@endif
    <div class="grid gap-6 xl:grid-cols-3"><div class="space-y-6 xl:col-span-2"><div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><h3 class="text-lg font-bold">Unit Information</h3><dl class="mt-6 grid gap-5 sm:grid-cols-2">@foreach([['Item',$itemUnit->item->display_name],['Category',$itemUnit->item->category->name],['Serial Number',$itemUnit->serial_number ?: 'Not provided'],['Property Number',$itemUnit->property_number ?: 'Not provided'],['Condition',ucwords(str_replace('_',' ',$itemUnit->condition))],['Availability',ucfirst($itemUnit->availability_status)],['Location',$itemUnit->location ?: $itemUnit->item->location ?: 'Not assigned'],['Acquisition Date',$itemUnit->acquisition_date?->format('F j, Y') ?: 'Not provided'],['Acquisition Cost',$itemUnit->acquisition_cost ? '₱'.number_format((float)$itemUnit->acquisition_cost,2) : 'Not provided']] as [$label,$value])<div><dt class="text-xs font-bold uppercase tracking-wide text-gray-500">{{ $label }}</dt><dd class="mt-1 font-semibold text-gray-900">{{ $value }}</dd></div>@endforeach</dl>@if($itemUnit->remarks)<div class="mt-6 border-t pt-5"><p class="text-xs font-bold uppercase text-gray-500">Remarks</p><p class="mt-2 text-gray-700">{{ $itemUnit->remarks }}</p></div>@endif</div>
        @can('archive items')@unless(in_array($itemUnit->availability_status,['reserved','borrowed']))<div class="rounded-2xl border border-red-200 bg-white p-6"><h3 class="font-bold text-red-800">Archive Equipment Unit</h3><p class="mt-1 text-sm text-gray-600">The unit can be restored later from Archived Units.</p><form method="POST" action="{{ route('item-units.destroy',$itemUnit) }}" class="mt-4" onsubmit="return confirm('Archive this equipment unit?')">@csrf @method('DELETE')<button class="rounded-xl bg-red-600 px-4 py-2.5 font-semibold text-white">Archive Unit</button></form></div>@endunless @endcan</div>
        <div class="space-y-6"><div class="rounded-2xl border border-gray-200 bg-white p-6 text-center shadow-sm"><h3 class="font-bold text-gray-900">Barcode Label</h3><div class="mt-5 w-full">{!! $barcodeSvg !!}</div><p class="mt-3 font-mono text-sm font-bold tracking-widest">{{ $itemUnit->barcode_value }}</p></div><div class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><h3 class="font-bold">Audit Details</h3><p class="mt-4 text-sm text-gray-600">Created by <strong>{{ $itemUnit->creator?->full_name ?? 'System' }}</strong></p><p class="mt-2 text-sm text-gray-600">Last updated {{ $itemUnit->updated_at->diffForHumans() }}</p><a href="{{ route('items.show',$itemUnit->item) }}" class="mt-5 inline-block font-semibold text-green-700">View parent item →</a></div></div></div>
</x-app-layout>
