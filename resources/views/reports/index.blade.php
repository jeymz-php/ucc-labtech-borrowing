<x-app-layout>
<x-slot name="header"><div><h1 class="text-xl font-bold text-gray-900">Reports & Analytics</h1><p class="mt-1 text-sm text-gray-500">Borrowing activity, utilization, and inventory status.</p></div></x-slot>
<div class="space-y-6">
<form method="GET" class="flex flex-wrap items-end gap-3 rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div><label class="text-xs font-semibold uppercase text-gray-500">From</label><input type="date" name="from" value="{{ $from->format('Y-m-d') }}" class="mt-1 block rounded-xl border-gray-300"></div><div><label class="text-xs font-semibold uppercase text-gray-500">To</label><input type="date" name="to" value="{{ $to->format('Y-m-d') }}" class="mt-1 block rounded-xl border-gray-300"></div><button class="rounded-xl bg-green-700 px-5 py-2.5 text-sm font-semibold text-white">Apply</button>@can('export reports')<a href="{{ route('reports.export',['from'=>$from->format('Y-m-d'),'to'=>$to->format('Y-m-d')]) }}" class="rounded-xl border border-green-700 px-5 py-2.5 text-sm font-semibold text-green-700">Export CSV</a><a href="{{ route('reports.export.pdf',['from'=>$from->format('Y-m-d'),'to'=>$to->format('Y-m-d')]) }}" class="rounded-xl bg-red-600 px-5 py-2.5 text-sm font-semibold text-white">Generate PDF</a>@endcan</form>
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach(['total'=>'Total Requests','active'=>'Active','returned'=>'Returned','overdue'=>'Overdue','available_units'=>'Available Units','maintenance_units'=>'Maintenance','lost_units'=>'Lost'] as $key=>$label)<div class="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><div class="text-sm text-gray-500">{{ $label }}</div><div class="mt-2 text-3xl font-bold text-gray-900">{{ number_format($summary[$key]) }}</div></div>@endforeach</div>
<div class="grid gap-6 xl:grid-cols-2"><section class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><h2 class="font-bold text-gray-900">Top Borrowed Equipment</h2><div class="mt-4 space-y-3">@forelse($topItems as $item)<div class="flex justify-between border-b border-gray-100 pb-3 text-sm"><span>{{ $item->name }}</span><strong>{{ $item->borrow_count }}</strong></div>@empty<p class="text-sm text-gray-500">No data for this range.</p>@endforelse</div></section><section class="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><h2 class="font-bold text-gray-900">Top Borrowers</h2><div class="mt-4 space-y-3">@forelse($topBorrowers as $user)<div class="flex justify-between border-b border-gray-100 pb-3 text-sm"><span>{{ $user->full_name }}</span><strong>{{ $user->borrow_count }}</strong></div>@empty<p class="text-sm text-gray-500">No data for this range.</p>@endforelse</div></section></div>
<section class="overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm"><div class="border-b px-6 py-5"><h2 class="font-bold text-gray-900">Recent Transactions</h2></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th class="px-5 py-3">Code</th><th class="px-5 py-3">Borrower</th><th class="px-5 py-3">Status</th><th class="px-5 py-3">Created</th></tr></thead><tbody class="divide-y">@foreach($recent as $row)<tr><td class="px-5 py-4"><a class="font-semibold text-green-700" href="{{ route('borrowings.show',$row) }}">{{ $row->borrowing_code }}</a></td><td class="px-5 py-4">{{ $row->borrower_name }}</td><td class="px-5 py-4 uppercase">{{ $row->status }}</td><td class="px-5 py-4">{{ $row->created_at->format('M d, Y') }}</td></tr>@endforeach</tbody></table></div></section>
</div></x-app-layout>